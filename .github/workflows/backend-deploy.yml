name: Deploy Backend to Server

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Disable sparse checkout (GitHub Actions bug fix)
        run: |
          git config core.sparseCheckout false
          git config --worktree core.sparseCheckout false 2>/dev/null || true
          git config --unset extensions.worktreeConfig 2>/dev/null || true
          git read-tree -mu HEAD

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DOCS_SSH_HOST }}
          username: ${{ secrets.DOCS_SSH_USER }}
          key: ${{ secrets.DOCS_SSH_KEY }}
          script: |
            /usr/local/bin/deploy-backend.sh
